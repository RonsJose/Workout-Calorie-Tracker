package ie.atu.microservice.homepage.calorieMicro;

public class foodCal {
    /// Allow user to add food they have eaten (send to calculatorCal)
    /// Allow user to plan diet
    /// Allow user to add own calories if not in given list
}
