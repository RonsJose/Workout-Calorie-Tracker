package ie.atu.workoutcalorietracker.Model;

public class Foods {
    /// Allow user to add food they have eaten (send to controller)
    /// Allow user to plan diet
    /// Allow user to add own calories if not in given list
}
